VITE_API_BASE_URL=http://localhost:3000
VITE_APP_NAME=LegalOps

# Optional: tenant `code` for public branding on /welcome when users open / without ?code=
# VITE_DEFAULT_TENANT_CODE=demo

# Keycloak (HLD aligned)
VITE_KEYCLOAK_URL=http://localhost/auth
VITE_KEYCLOAK_REALM=legal-opinion-saas
VITE_KEYCLOAK_CLIENT_ID=legal-opinion-web

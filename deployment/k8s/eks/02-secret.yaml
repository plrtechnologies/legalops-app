# IMPORTANT: Do not store real secrets in git.
# Use AWS Secrets Manager + External Secrets Operator, or Sealed Secrets.
apiVersion: v1
kind: Secret
metadata:
  name: legalops-secrets
  namespace: legalops
type: Opaque
data:
  DB_PASSWORD: REPLACE_WITH_BASE64
  JWT_SECRET: REPLACE_WITH_BASE64
  JWT_REFRESH_SECRET: REPLACE_WITH_BASE64
  SARVAM_API_KEY: REPLACE_WITH_BASE64
  KEYCLOAK_ADMIN_PASSWORD: REPLACE_WITH_BASE64
  KEYCLOAK_DB_PASSWORD: REPLACE_WITH_BASE64
